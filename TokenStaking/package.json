{
  "name": "token-staking-subgraph",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ TokenStaking",
    "create-local": "graph create --node http://localhost:8020/ TokenStaking",
    "remove-local": "graph remove --node http://localhost:8020/ TokenStaking",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 TokenStaking"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
